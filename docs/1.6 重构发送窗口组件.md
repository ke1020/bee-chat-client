大家好，本节视频我们来重构输入框组件。
也就是这一部分。
在 。components。Chat。目录下新建 。Sender。 目录。
输入框组件将有几个子组件组合而成。
每个子组件都是一个文件。
我们会将它们统一放在 。Sender。 目录下。
在 .Sender。 目录下新建 。Index。tsx。文件。
这是输入框组件的入口文件。
在首页找到 。Sender。组件 UI 部分。
agent。Name。指的是这一部分。
我们把它作为输入框组件的一部分。
复制 。agent。Name。与 。Sender。组件外层包裹的 。d。i。v。 元素。
粘贴到 。Sender。Index 函数组件中返回。
导入相应的对象或组件数据类型。
从 。locals。引入 。locale。本地化对象。
从 。themes。引入 。styles。对象。
从 。messages。引入 。messages。对象。
从。conversations。引入 。cur。conversations。对象。
从 。messages。引入 on。Request。about。函数。
从。conversations。引入 。set。active。conversation。函数。
把管理深度思考状态的部分从首页复制过来。
导入。use。state 钩子函数。
把词槽配置从首页复制过来。
词槽指的是输入框输入的内容部分。
第一部分类型为 。text 。对应这里。
第二部分类型为 。select 。组件对应这里。
第三部分类型为 。text。对应这里。
导入。Sender。Props。接口。
is。Requesting。在。messages。模型中。
为组件定义一个接口。
把。list。Ref。作为组件参数传递进来。
sender。Ref。是输入框组件的 DOM 引用对象。
我们在组件内定义。
从首页复制过来。
导入 。use。Ref。钩子函数。
导入。Get。Ref。类型。
回到首页。
删掉。Sender。组件相关的导入。
删掉词槽配置。
use。Effect。部分剪切到输入框组件中。
删掉。Sender。组件 。UI。 部分。
导入自定义的输入框组件。
删掉失效的组件。类型或数据对象导入。
输入框需要。list。Ref。参数。
切换到浏览器进行预览。

接下来，我们把输入框组件的脚步继续拆分一下。
脚步指的是这一部分。
复制。Footer。UI。部分代码。
在同目录下新建。Footer。tsx。文件。
把。Footer。部分。UI。粘贴进来。
导入相关接口与类型。
引入 。locale。对象。
深度思考的状态管理复制过来。
导入。use。State。
为脚步定义一个接口。
action。Node。从父组件通过参数传入。
接口添加到组件参数中。
删掉输入框组件的 Footer。UI。部分。
从自定义。Footer。组件导入。
把 。action。Node。参数传递给子组件。
删掉深度思考的状态管理。
删掉无效导入。
切换到浏览器预览一下。

未来输入框组件会加入附件上传等功能。

本节就到这里。
