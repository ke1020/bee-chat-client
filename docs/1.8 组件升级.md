大家好，当前 ant。design。x 已经升级到 2点2点1 版本。
并且。ant。d。组件也升级到了 6点2点2 版本
本节我们来将这几个组件升级一下。
打开项目。
在终端输入升级命令。
回车执行。
安装完毕后。
运行项目。
本次升级。Bubble。List。组件存在样式调整。
我们希望还是按照原来的样式显示。
所以。这里使用 ant。d。style。库的 。create。Styles 函数。
来创建一个自定义样式。
通过开发者工具对。Bubble。List。组件进行分析。
我们可以给。Bubble。List。组件滚动内容区。
添加一个 840 像素的固定宽度。
也就是之前设置到 bubble 上的宽度。
然后给滚动内容区父元素设置。align。items。center。
让滚动内容区水平居中就可以了。
最后把样式绑定到。Bubble。List。组件的 class。Name 属性上。
再次到浏览器预览。
对话消息列表就恢复到之前的样式了。
以上就是本节内容。
